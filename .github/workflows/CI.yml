name: CI

on:
  pull_request:

permissions:
  contents: read

jobs:
  build_ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v7

      - name: Install dependencies
        run: npm install

      - name: Build Next.js app
        run: npm run build